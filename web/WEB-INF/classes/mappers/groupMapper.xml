<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="cn.sams.dao.score.GroupManagementDao">

    <select id="queryStu" resultType="cn.sams.entity.Student">
        SELECT * FROM sams_student
    </select>

    <select id="queryTeacherIdByInfo" parameterType="string" resultType="cn.sams.entity.Course">
        SELECT t.cou_tea_no FROM sams_course t WHERE t.course_id = #{0} AND t.cou_place_id = #{1} AND t.cou_week = #{2}
        AND t.cou_day_of_week = #{3} AND t.cou_time = #{4}
    </select>

    <select id="queryTeacherName" parameterType="string" resultType="cn.sams.entity.Teacher">
        select tea_name from sams_teacher where tea_no = #{0}
    </select>

</mapper>
